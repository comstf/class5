<!DOCTYPE html>
<html lang="si">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>පොඩ්ඩන්ගේ සෙල්ලම් ලෝකය</title>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --home-color: #FF9F43;
            --game1-color: #FF6B6B;
            --game2-color: #1DD1A1;
            --game3-color: #54A0FF;
            --game4-color: #FECA57;
            --game5-color: #A29BFE;
            --white: #ffffff;
        }

        body {
            font-family: 'Fredoka', sans-serif;
            background-color: #e0f7fa;
            margin: 0;
            padding: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow-x: hidden;
            background-image: url('https://www.transparenttextures.com/patterns/white-prism.png');
        }

        .main-container {
            background: var(--white);
            width: 100%;
            max-width: 850px;
            border-radius: 50px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.15);
            overflow: hidden;
            border: 8px solid #FFD93D;
            z-index: 10;
            position: relative;
        }

        /* --- Tabs Wrapper with Arrows --- */
        .tabs-wrapper {
            display: flex;
            align-items: center;
            background: #fffbe6;
            padding: 15px 10px;
            position: relative;
            border-bottom: 5px dashed #FFD93D;
        }

        .nav-tabs {
            display: flex;
            overflow-x: auto;
            scroll-behavior: smooth;
            gap: 15px;
            padding: 10px;
            scrollbar-width: none;
            -ms-overflow-style: none;
        }

        .nav-tabs::-webkit-scrollbar { display: none; }

        /* Arrow Buttons Style */
        .arrow-btn {
            background: #FFD93D;
            border: none;
            color: #fff;
            font-size: 30px;
            font-weight: bold;
            cursor: pointer;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 4px 0px #e67e22;
            transition: 0.2s;
            z-index: 20;
            user-select: none;
        }

        .arrow-btn:active {
            transform: translateY(3px);
            box-shadow: none;
        }

        .tab-btn {
            padding: 15px 25px;
            border: none;
            cursor: pointer;
            border-radius: 25px;
            font-family: 'Fredoka', sans-serif;
            font-size: 18px;
            font-weight: 700;
            color: white;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            flex-shrink: 0;
            box-shadow: 0 6px 0px rgba(0,0,0,0.1);
            text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        /* Button Colors */
        .btn-home { background: var(--home-color); border-bottom: 5px solid #e67e22; }
        .btn-g1 { background: var(--game1-color); border-bottom: 5px solid #ee5253; }
        .btn-g2 { background: var(--game2-color); border-bottom: 5px solid #10ac84; }
        .btn-g3 { background: var(--game3-color); border-bottom: 5px solid #2e86de; }
        .btn-g4 { background: var(--game4-color); border-bottom: 5px solid #f39c12; }
        .btn-g5 { background: var(--game5-color); border-bottom: 5px solid #8e44ad; }

        .tab-btn.active {
            transform: translateY(4px);
            box-shadow: 0 2px 0px rgba(0,0,0,0.1);
        }

        /* Content Area */
        .content {
            padding: 50px 20px;
            display: none;
            text-align: center;
            animation: bounceIn 0.6s ease;
        }

        .content.active { display: block; }

        @keyframes bounceIn {
            0% { transform: scale(0.3); opacity: 0; }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); opacity: 1; }
        }

        .icon-large { font-size: 100px; margin-bottom: 20px; display: block; }
        .play-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 18px 50px;
            font-size: 24px;
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            box-shadow: 0 8px 0px rgba(0,0,0,0.15);
        }

        @media (max-width: 480px) {
            .arrow-btn { width: 40px; height: 40px; font-size: 20px; }
            .tab-btn { font-size: 16px; padding: 10px 15px; }
        }
    </style>
</head>
<body>

    <div class="main-container">
        <div class="tabs-wrapper">
            <button class="arrow-btn" onclick="scrollTabs(-100)">◀</button>
            
            <div class="nav-tabs" id="tabContainer">
                <button class="tab-btn btn-home active" onclick="showGame(event, 'home')">🏠 Home</button>
                <button class="tab-btn btn-g1" onclick="showGame(event, 'game1')">🦁 Game 1</button>
                <button class="tab-btn btn-g2" onclick="showGame(event, 'game2')">🔢 Game 2</button>
                <button class="tab-btn btn-g3" onclick="showGame(event, 'game3')">🍎 Game 3</button>
                <button class="tab-btn btn-g4" onclick="showGame(event, 'game4')">🎨 Game 4</button>
                <button class="tab-btn btn-g5" onclick="showGame(event, 'game5')">🧩 Game 5</button>
            </div>

            <button class="arrow-btn" onclick="scrollTabs(100)">▶</button>
        </div>

        <div id="home" class="content active">
            <span class="icon-large">🏠</span>
            <h1 style="color: var(--home-color);">Welcome Childrens!</h1>
            <p>Ready for a fun adventure? Pick a English with game!</p>
        </div>

        <div id="2026.02.16 game1" class="content">
            <span class="icon-large">🦁</span>
            <h1 style="color: var(--game1-color);">Fill Correct Lettrs</h1>
            <a href="https://lakdiva.github.io/Classgame/" class="play-btn" style="background: var(--game1-color);">START 🚀</a>
        </div>

        <div id="2026.02.22" class="content">
            <span class="icon-large">🔢</span>
            <h1 style="color: var(--game2-color);">Words Fun</h1>
            <a href="  " class="play-btn" style="background: var(--game2-color);">START 🔢</a>
        </div>

        <div id="game3" class="content">
            <span class="icon-large">🍎</span>
            <h1 style="color: var(--game3-color);">TIME FUN</h1>
            <a href=" " class="play-btn" style="background: var(--game3-color);">START 🍎</a>
        </div>

        <div id="game4" class="content">
            <span class="icon-large">🎨</span>
            <h1 style="color: var(--game4-color);">අකුරු හුරුව</h1>
            <a href=" " class="play-btn" style="background: var(--game4-color);">START 🎨</a>
        </div>

        <div id="game5" class="content">
            <span class="icon-large">🧩</span>
            <h1 style="color: var(--game5-color);">Puzzle Solver</h1>
            <a href="#" class="play-btn" style="background: var(--game5-color);">START 🧩</a>
        </div>
    </div>

    <script>
        // Tab scrolling function
        function scrollTabs(distance) {
            document.getElementById('tabContainer').scrollBy({
                left: distance,
                behavior: 'smooth'
            });
        }

        // Section switching function
        function showGame(evt, gameId) {
            const sections = document.querySelectorAll('.content');
            sections.forEach(s => s.classList.remove('active'));

            const buttons = document.querySelectorAll('.tab-btn');
            buttons.forEach(b => b.classList.remove('active'));

            document.getElementById(gameId).classList.add('active');
            evt.currentTarget.classList.add('active');
        }
    </script>

</body>
</html>
